/*
 * physcon.hpp
 *
 *  Created on: Apr 30, 2018
 *      Author: dmarce1
 */

#ifndef PHYSCON_HPP_
#define PHYSCON_HPP_

#include "real.hpp"

constexpr real kb = 1.38064852e-16;
constexpr real me = 9.10938356e-28;
constexpr real c = 2.99792458e+10;
constexpr real h = 6.626070040e-27;
constexpr real amu = 1.660539040e-24;
constexpr real zero = real(0);
constexpr real one = real(1);
constexpr real two = real(2);
constexpr real three = real(3);
constexpr real three_inv = one / three;
constexpr real four = real(4);
constexpr real five = real(5);
constexpr real half = one / two;
constexpr real eV = 1.6021772e-12;
constexpr real esu = 4.8032068e-10;

#endif /* PHYSCON_HPP_ */
